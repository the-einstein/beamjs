#!/bin/bash
# BeamJS - JavaScript/TypeScript runtime on the BEAM VM

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

cd "$SCRIPT_DIR"
mix run --no-halt -e "BeamjsCli.main(System.argv())" -- "$@"
